# SSL Certificate Alert Configuration
# Copy this file to config.ini and customize for your environment

[email]
# Sender email address (required)
from = alerts@company.com

# Recipient email address (required)
to = admin@company.com

[alerts]
# Comma-separated list of days before expiration to send alerts
# Default: 14,7,3,0 (alerts at 14, 7, 3 days before expiry, and on expiration day)
warning_days = 7,14,30

